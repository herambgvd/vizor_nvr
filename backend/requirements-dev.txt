# =============================================================================
# Dev / Test Dependencies
# =============================================================================
# Install: pip install -r requirements-dev.txt
# Container image excludes these — keeps prod image small.
# =============================================================================

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
pytest-mock>=3.12
aiosqlite>=0.20         # SQLite async driver for unit tests
fakeredis[lua]>=2.20    # In-process Redis for bridge tests
httpx>=0.27             # Async HTTP client for testing FastAPI
freezegun>=1.4          # Time control in tests

# Linting / formatting
ruff>=0.5
black>=24.0

# Type checking
mypy>=1.10
types-requests
types-redis

# Coverage reporting
coverage>=7.0
