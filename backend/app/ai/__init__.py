# AI domain package — FRS, Vizor Query, models, webhooks, scenarios,
# Metropolis adapters. Lives alongside the NVR-native domains (cameras,
# recordings, events) but encapsulates all AI/analytics business logic.
