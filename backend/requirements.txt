# =============================================================================
# GVD NVR v2.0 — Runtime Dependencies
# =============================================================================

# ── Core ────────────────────────────────────────────────────────────
fastapi
uvicorn
pydantic
pydantic[email]      # EmailStr support
email-validator
python-dotenv
python-multipart
PyYAML

# ── Database ────────────────────────────────────────────────────────
SQLAlchemy
asyncpg
alembic
psycopg2-binary

# ── Mongo (used ONLY by scripts/migrate_from_vizor_app.py during cutover) ─
motor

# ── Auth & Security ─────────────────────────────────────────────────
# bcrypt pinned <4.1 — passlib 1.7.4 reads version via removed
# `__about__` attr in 4.1+, breaking password hashing.
bcrypt>=4.0,<4.1
passlib
python-jose
cryptography

# ── HTTP / Networking ───────────────────────────────────────────────
httpx

# ── Redis (Metropolis bridge + rate-limit + DLQ) ───────────────────
redis>=5.0

# ── Cloud Storage (S3/RustFS/MinIO) ────────────────────────────────
boto3

# ── System monitoring ──────────────────────────────────────────────
psutil

# ── Observability (Prometheus metrics) ─────────────────────────────
prometheus-client
prometheus-fastapi-instrumentator

# ── ONVIF (optional — install for ONVIF discovery + PTZ) ──────────
onvif-zeep
WSDiscovery

# =============================================================================
# Dev Dependencies (install separately: pip install -r requirements-dev.txt)
# =============================================================================
# black==26.1.0
# flake8==7.3.0
# isort==8.0.0
# mypy==1.19.1
# pytest==9.0.2
