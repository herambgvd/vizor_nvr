fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
requests==2.32.3
pillow==11.0.0
qdrant-client==1.12.1
numpy==1.26.4
psycopg2-binary==2.9.10
# Inference runs on the shared Triton server — plugin is a thin client.
# onnxruntime (CPU) kept only as a dev fallback when INFERENCE_BACKEND != triton.
onnxruntime==1.20.1
tritonclient[http]==2.50.0
