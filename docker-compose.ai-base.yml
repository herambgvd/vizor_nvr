# =============================================================================
# Vizor NVR — AI Shared Infra (Triton · Qdrant · RustFS)
# =============================================================================
# docker compose -f docker-compose.yml -f docker-compose.ai-base.yml up -d
# The `internal` network is created by the NVR core (docker-compose.yml); these
# plugin overlays attach to it as an external network.
# =============================================================================

services:
  triton:
    image: nvcr.io/nvidia/tritonserver:24.08-py3
    container_name: gvd_ai_triton
    restart: unless-stopped
    # Explicit model control: load ONLY the models listed below, so the GPU isn't
    # filled by every scenario's weights. All scenario detectors run on TensorRT
    # FP16 engines (*_trt) for ~2-3x faster inference; the matching .plan files must
    # be built first (triton/build_trt_engines.sh). Switch to
    # --model-control-mode=poll to auto-load the whole repo.
    command: >
      tritonserver
      --model-repository=/models
      --model-control-mode=explicit
      --load-model=ppe_yolo26_trt
      --load-model=person_reid_trt
      --load-model=scrfd_10g
      --load-model=arcface_r50
      --load-model=antispoofing
      --load-model=fairface
      --log-verbose=0
      --strict-model-config=false
    networks:
      - internal
    volumes:
      - ./triton/model_repository:/models:ro
    shm_size: "1g"
    deploy:
      resources:
        limits:
          memory: ${TRITON_MEM_LIMIT:-8g}
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "bash", "-lc", "exec 3<>/dev/tcp/127.0.0.1/8000"]
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 60s

  # qdrant REMOVED — FRS face embeddings now live in the FRS Postgres via pgvector
  # (scenarios/frs/qdrant/store.py rewritten to a pgvector backend; the main db image
  # is timescaledb-ha which bundles pgvector). No separate vector DB container.

  # ai-redis REMOVED — the AI event bus + control plane now live on the core `redis`
  # (db1), which is configured noeviction + AOF so the ai:events stream is durable and
  # never evicted. AI services use AI_REDIS_URL=redis://redis:6379/1.

  # rustfs (S3 object store) REMOVED — AI media now lives on a local-disk volume
  # via vizor_sdk.objectstore.LocalObjectStore (STORAGE_BACKEND=local). The shared
  # `objstore` volume is mounted into the FRS/PPE services at /data/objstore.

networks:
  internal:
    external: true
    name: ${COMPOSE_PROJECT_NAME:-vizor_nvr}_internal

volumes:
