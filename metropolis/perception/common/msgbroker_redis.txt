# nvmsgbroker config — sends detection events to Redis Stream the
# Metropolis bridge subscribes to. Same Redis instance the rest of
# Vizor uses (cache + queue + bridge).
#
# Stream name MUST match METROPOLIS_STREAM in bridge env.

[message-broker]
proto-lib=/opt/nvidia/deepstream/deepstream/lib/libnvds_redis_proto.so
conn-str=redis;6379
topic=metropolis:events
# Adapter expects a "payload" Redis Stream field with JSON-encoded body
sync=0
config-file=msgbroker_redis_extras.txt

[message-broker-config]
# Reconnect aggressively if Redis blips
reconnect-attempt-count=10
reconnect-attempt-sleep-sec=2

# Schema: Metropolis canonical JSON. Bridge translates to NVR ingest
# format. Do NOT change without coordinating with metropolis_bridge.py
# message-converter-config knobs lives in scenario-specific configs.
