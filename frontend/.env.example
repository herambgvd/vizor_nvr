# Same-origin defaults (production / docker). Override only for a
# local CRA dev server talking to a backend on a different host.
REACT_APP_BACKEND_URL=
REACT_APP_API_URL=/api
REACT_APP_GO2RTC_URL=/go2rtc
